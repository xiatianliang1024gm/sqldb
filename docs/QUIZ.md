Q1（边界 · 前缀无歧义）​ EncodeKey 第三铁律是「两个不同值的编码互不为前缀」。用 TEXT 主键说明：为什么字符串编码必须以 0x00 0x00 收尾、内容里的 0x00 必须转义成 0x00 0xFF？如果违反这条（比如不转义、或用单 0x00 收尾），对 "ab" 和 "abc" 这种前缀串做 WHERE name >= 'ab' 的范围扫描，会出现多读还是漏读，还是别的后果？
答：

Q2（反直觉 · ±0.0）​ FLOAT 编码里 -0.0 必须和 +0.0 编成同一串字节，否则 DecodeKey 会还原出 NaN。代码里 if f == 0 { f = 0 } 这一行到底在干什么、为什么不能删？如果只保留后面的 if f < 0 分支、删掉这行，-0.0 会被编成什么、解码出来是什么？

Q3（量化 · 符号位翻转）​ INT 主键编码是「1 字节标签 + 8 字节大端 + uint64(v) ^ (1<<63)」。写出 -1 和 1 各自经过翻转后的 64 位模式，并证明 -1 的编码字节序严格小于 1 的编码字节序。另外：为什么这里用定长 9 字节而不是更省空间的变长编码？